import { COMFORT, accelerationBand } from '../config/comfort';
export function BigBar({value}:{value:number}){const amount=Math.min(1,Math.abs(value)/COMFORT.acceleration.displayMax);const positive=value>=0;const band=accelerationBand(value);return <div className="bar-shell" aria-label={`Acceleration ${value.toFixed(1)} metres per second squared`}><div className="zone zero"/><div className="bar-fill" data-band={band} style={{height:`${amount*50}%`,bottom:positive?'50%':'auto',top:positive?'auto':'50%'}}/><span className="bar-label top">ACCELERATE</span><span className="bar-label bottom">BRAKE</span><span className="zero-line"/></div>}
